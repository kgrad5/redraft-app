# Values are read by both the app (pydantic-settings) and docker compose.
# Unquoted KEY=value only — the Makefile sources this file directly.

POSTGRES_USER=redraft
POSTGRES_PASSWORD=redraft
POSTGRES_DB=redraft
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

APP_HOST=127.0.0.1
APP_PORT=8000
